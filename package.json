{
  "name": "updatetest",
  "version": "1.0.3",
  "description": "testing udpate for electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build  --win --publish never",
    "deploy": "electron-builder build  --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bksubhuti/updatetest.git"
  },
  "author": "bksubhuti",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.1.2",
    "electron-builder": "^22.8.0"
  },
  "dependencies": {
    "electron-updater": "^4.3.4"
  }
}
